---
export interface Props extends astroHTML.JSX.AnchorHTMLAttributes {}

const { class: className } = Astro.props;
---

<div
    class={`mb-6 sm:p-8 sm:bg-white dark:sm:bg-slate-800 sm:shadow-xl rounded-lg ${
        className ? className : ""
    }`}
>
    <slot />
</div>
